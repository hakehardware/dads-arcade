import type { Metadata } from "next";
import { Geist_Mono } from "next/font/google";
import "./globals.css";

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Dad's Arcade | ASCII Gaming Cabinet",
  description: "A retro-styled web arcade cabinet hosting ASCII-based games with classic green-on-black terminal aesthetic.",
  keywords: ["arcade", "ascii", "retro", "games", "terminal"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${geistMono.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
